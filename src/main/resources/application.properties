#Mysql Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/expensemanager1
spring.datasource.username=root
spring.datasource.password=An@ny@613M
server.servlet.context-path=/api/v1.0

#JPA Configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#Email configuration

# Email Configuration (Brevo SMTP)

spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587

spring.mail.username=${BREVO_USERNAME}
spring.mail.password=${BREVO_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp

spring.mail.properties.mail.smtp.from=${BREVO_FROM_EMAIL}
jwt.secret=MyVeryStrongSecretKeyForExpenseManagerProject2026SecretKey
jwt.expiration=86400000


##app.activation.url=http://localhost:8080/activate
##money.manager.frontend.url=http://localhost:8080

money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL}

app.activation.url=${MONEY_MANAGER_BACKEND_URL}
